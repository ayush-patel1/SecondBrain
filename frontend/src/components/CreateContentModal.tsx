import { useState } from "react"

function CreateContentModal({open,onClose}) {
   
   return (
     <div>
       <CrossIcon onClick={(
        
       )}/>
     </div>
  )
}

export default CreateContentModal
